import 'package:flutter/material.dart';

class BotttomNavBar extends StatelessWidget {
  const BotttomNavBar({super.key});

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}